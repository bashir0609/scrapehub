# Production Environment Variables
# Copy all of this and paste it into Coolify's "Environment Variables" -> "Paste .env" section.

# Django Settings
DEBUG=0
SECRET_KEY=change-this-to-a-long-random-string-in-production
ALLOWED_HOSTS=your-domain.com,www.your-domain.com,localhost

# Database (PostgreSQL)
# Note: In Docker/Coolify, the host is the service name 'db', not localhost
POSTGRES_DB=scrapehub
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-this-password-to-something-secure
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Redis
REDIS_HOST=redis
